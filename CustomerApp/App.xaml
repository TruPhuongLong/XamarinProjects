<?xml version="1.0" encoding="utf-8"?>
<Application 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="CustomerApp.App">
    
    <Application.Resources>
        <ResourceDictionary>
            
          <ControlTemplate x:Key="MainPageTemplate">
            <AbsoluteLayout>

                <ContentPresenter AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All"/>

                <ActivityIndicator 
                    AbsoluteLayout.LayoutBounds="0,0,1,1" 
                    AbsoluteLayout.LayoutFlags="All" 
                    HorizontalOptions="CenterAndExpand" 
                    VerticalOptions="CenterAndExpand"
                    Color="Green"
                    IsVisible="{TemplateBinding BindingContext.CustomerStore.State.IsRunningIndicator}"
                    IsRunning="{TemplateBinding BindingContext.CustomerStore.State.IsRunningIndicator}"
                />

                <Frame x:Name="ViewNotification" Padding="20" BackgroundColor="Silver" AbsoluteLayout.LayoutBounds="2.0,80,AutoSize,AutoSize" AbsoluteLayout.LayoutFlags="XProportional" >
                    <Label Text="im notification" />
                </Frame>
                
            </AbsoluteLayout>
          </ControlTemplate>
            
        </ResourceDictionary>
    </Application.Resources>
    
</Application>