<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="CustomerApp.src.Views.Components.LoginView"
    Padding="40, 60"
    WidthRequest="280"
    HeightRequest="250"
    BackgroundColor="Lime"
    >

    <StackLayout Spacing="20">
        <!--<Entry Placeholder="@email" Text="{Binding UserName, Mode=TwoWay}" />
        <Entry Placeholder="@password" Text="{Binding Password, Mode=TwoWay}"/>-->
        <Button Clicked="OnLoginClicked" Margin="0, 40, 0, 0" Text="Login"></Button>
    </StackLayout>
        
</ContentView>

