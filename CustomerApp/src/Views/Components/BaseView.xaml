<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="CustomerApp.src.Views.Components.BaseView"
    x:Name="this"
    >

   <!-- <StackLayout>
        <Label Text="fsfsfsfs"/>
        <ContentPresenter Content="{Binding BaseContent, Source={x:Reference this}}"/>
    </StackLayout>-->

    <AbsoluteLayout>
        <Frame  AbsoluteLayout.LayoutBounds="1.1,0.2,200,80"  AbsoluteLayout.LayoutFlags="PositionProportional">
            <Label />
        </Frame>
        <ContentPresenter Content="{Binding BaseContent, Source={x:Reference this}}" AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All"/>
    </AbsoluteLayout>

    
</ContentView>
