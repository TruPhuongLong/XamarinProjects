<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="CustomerApp.src.Views.Components.BasePage"
    x:Name="BasePage"
    >
    
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness"
                    iOS="0, 20, 0, 0"/>
    </ContentPage.Padding>

    <AbsoluteLayout BindingContext="{Binding Source={x:Reference BasePage}}">

        <ContentPresenter Content="{Binding BaseContentPage}" AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All"/>

        <ActivityIndicator 
            AbsoluteLayout.LayoutBounds="0,0,1,1" 
            AbsoluteLayout.LayoutFlags="All" 
            HorizontalOptions="CenterAndExpand" 
            VerticalOptions="CenterAndExpand"
            Color="Green"
            IsRunning="true"
        />

        <Frame Padding="50" AbsoluteLayout.LayoutBounds="1,80,AutoSize,AutoSize" AbsoluteLayout.LayoutFlags="XProportional">
            <Label Text="im notification"/>
        </Frame>
        
    </AbsoluteLayout>
    
    
</ContentPage>
